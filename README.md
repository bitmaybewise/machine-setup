# Machine setup

## MacOS

Run `./brew-install.sh` to install packages to a new machine.

Run `./brew-listupdate.sh` to get all packages installed and update the `brewlist.txt` and `brewcasks.txt` files with current system state.
